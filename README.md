# Backport for concat 0.2.4

This is a backport for 0.2.4 that i've used for some time, just leave it here because it's not broken

## Features

- detect existing chunks when download was restarted
- updated API key
